# --- Dependencies ---
node_modules/
package-lock.json # Optional: Keep this line ONLY if you use yarn. If using npm, remove this line (usually you want to commit package-lock.json)

# --- Environment Variables (CRITICAL) ---
# You are using 'dotenv', so never commit these!
.env
.env.test
.env.production
.env.local

# --- Operating System (Windows 11) ---
Thumbs.db
ehthumbs.db
Desktop.ini

# --- Editor Config (VS Code) ---
.vscode/
*.code-workspace

# --- Logs & Debugging ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
*.pid.lock

# --- File Uploads (Multer) ---
# Since you use Multer, ignore local temp folders if you have them
uploads/
tmp/
dist/
coverage/   